<?php

module_load_include('module', 'era');
$pen = era_get_rates();

function currency_send_rate($pen){
	die(print_r($pen));
	$rate = $pen['rates']['PEN'];
	return $rate;
}

// $rate = $pen['rates']['PEN'];
	// die(print_r($rate));

function currency_exchange_preprocess(&$variables) {
	$rate = currency_send_rate($pen);
	die(print_r($rate));
  $variables['xpen'] = $rate;
	print_r($variables);
}









